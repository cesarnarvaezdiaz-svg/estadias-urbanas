# Backend Estadias Urbanas

Servidor Express liviano para exponer endpoints locales de ciudades, propiedades y salud del servicio.

## Comandos

```bash
npm install
npm run dev
```

Endpoints:

- `GET /api/health`
- `GET /api/cities`
- `GET /api/properties`
